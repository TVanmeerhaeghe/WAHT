model Realm {
  id              Int       @id
  slug            String    @unique
  name            String
  region          Region
  connectedRealms String[]
  lastSyncedAt    DateTime?

  snapshots AuctionSnapshot[]
  auctions  RawAuction[]
  alerts    Alert[]

  @@index([region])
  @@index([slug])
}
